---
const {
    size = 24,
    name = "icon",
    src = "sprite",
    className = "svg-icon",
} = Astro.props;
---

<svg
    class={`icon ${className}`}
    style="aspect-ratio: 1 / 1;"
    width={size}
    data-sprite
>
    <use xlink:href={`/${src}.svg#${name}`}></use>
</svg>
